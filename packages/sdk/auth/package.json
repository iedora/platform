{
  "name": "@iedora/auth-sdk",
  "version": "0.2.0",
  "description": "The one auth SDK: TypeScript client + JWKS verifier + service-token source (framework-agnostic, at '.'/'./tokens') AND the Next.js integration ('./next') — pre-wired for the shared iedora realm (getAccount, login/register/logout, SSO cookies). Products import auth from here; there is no separate nextjs package.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./tokens": "./src/tokens.ts",
    "./next": "./src/next/index.ts",
    "./next/middleware": "./src/next/middleware.ts",
    "./next/client": "./src/next/client.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "lint": "eslint . --cache --cache-location node_modules/.cache/.eslintcache"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "peerDependencies": {
    "next": "^16.2.6"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "next": "catalog:",
    "typescript": "catalog:",
    "@iedora/eslint-config": "workspace:*",
    "eslint": "catalog:",
    "typescript-eslint": "catalog:"
  }
}
