{
  "name": "@iedora/brand",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Iedora brand constants + cross-origin URL helpers shared across every product (apex, menu, core, future). Pure strings + URL builders — no runtime, no env validation, safe to import from server and client.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "devDependencies": {
    "@iedora/eslint-config": "workspace:*",
    "@types/node": "^22.19.19",
    "eslint": "^9.39.4",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.46.0",
    "vitest": "^4.1.7"
  }
}
