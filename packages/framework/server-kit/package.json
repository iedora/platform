{
  "name": "@iedora/server-kit",
  "version": "0.5.0",
  "description": "The shared Hono kernel every iedora service reuses: typed HttpError + error shaping, a zod validate() wrapper, request helpers (client IP / bearer token), and a bearer-auth middleware factory.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iedora/framework.git",
    "directory": "packages/framework/server-kit"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test",
    "lint": "eslint . --cache --cache-location node_modules/.cache/.eslintcache"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.9.0",
    "jose": "^5.9.6",
    "@node-rs/argon2": "^2.0.2"
  },
  "peerDependencies": {
    "hono": ">=4",
    "zod": "^3.25.0 || ^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "@types/node": "^22",
    "hono": "^4.12.30",
    "typescript": "catalog:",
    "zod": "^4.4.3",
    "@iedora/eslint-config": "workspace:*",
    "eslint": "catalog:",
    "typescript-eslint": "catalog:"
  }
}
