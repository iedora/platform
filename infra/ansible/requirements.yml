---
collections:
  - name: cloud.terraform
    version: ">=4.0.0,<5.0.0"
  - name: community.general
    version: ">=10.0.0"
  # ansible.posix.authorized_key (moved out of ansible.builtin)
  - name: ansible.posix
    version: ">=2.0.0"
