{
  "name": "@iedora/integration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cross-service integration tests: real Hono apps over real HTTP + real Postgres, exercising the contracts between services (e.g. menu's BillingClient against the billing service). Complements the per-service slice tests (which stub their peers).",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@iedora/contracts": "workspace:*",
    "@iedora/server-kit": "workspace:*",
    "@iedora/service-billing": "workspace:*",
    "@iedora/service-menu": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "jose": "catalog:",
    "kysely": "catalog:",
    "typescript": "catalog:"
  }
}
