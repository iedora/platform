/**
 * Re-export of `@iedora/brand`'s URL validators. Same import-path
 * compatibility note as `@/shared/brand` — prefer
 * `import { isSameOriginPath } from '@iedora/brand'` in new code.
 */
export { isSameOriginPath, isSameIedoraOrigin } from '@iedora/brand'
